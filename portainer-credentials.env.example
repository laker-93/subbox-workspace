# Copy to portainer-credentials.env (gitignored) and fill in. Used by
# .claude/skills/watch-container-logs/scripts/portainer_logs.py to log in to
# Portainer on staging/prod and fetch container logs.

PORTAINER_USERNAME=
PORTAINER_PASSWORD=
